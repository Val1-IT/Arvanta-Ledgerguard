# examples/investigations

Captured, deterministic investigation outputs (structured JSON + narrative) from
real runs of the conversion-error scenario. Used as documentation and as fixtures
for tests. Populated from FASE 5 onward.
