# examples/remediations

Captured remediation previews (proposed corrections + verification expectations)
produced by the FASE 4 financial integrity engine — preview only, nothing here is
executed. Regenerate with `npm run example`. Execution against real records happens
in FASE 6, after human approval; until then this is read-only planning output
sliced from the same engine run as `examples/investigations/`.
