{
  "name": "@ledgerguard/datahub",
  "version": "0.1.0",
  "description": "Optional DataHub catalog context and metadata write-back. Not part of execution authority.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Val1-IT/Arvanta-Ledgerguard.git",
    "directory": "packages/datahub"
  },
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src", "python"],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
