{
  "name": "@ledgerguard/policy",
  "version": "0.2.0",
  "description": "Deterministic policy decisions, trusted capability authority, approval gates, and safety audit primitives.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Val1-IT/Arvanta-Ledgerguard.git",
    "directory": "packages/policy"
  },
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ledgerguard/core": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
