# src/datahub/bootstrap

One-time DataHub metadata provisioning via the DataHub Python SDK / ingestion
recipes: datasets, schema, owners, glossary terms, tags, lineage, and demo
metadata. This is setup only — the agent does not read DataHub through this path.

Built in FASE 3A.
